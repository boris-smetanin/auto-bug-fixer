# utils/

Genuinely generic helpers that are used by **more than one domain**.

Rule: if a helper is specific to one domain (Spaces, Fix Attempts, Fix Loop, Drain, Logs, Settings) it stays in that domain's directory. Only lift it here when a second caller from a different domain appears.

Currently empty.
